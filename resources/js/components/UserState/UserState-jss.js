const styles = theme => ({
	userStateWidget: {
		display: 'flex',
		justifyContent: 'center',
		alignItems: 'center',
	},
	formControl: {
    margin: theme.spacing(1),
    minWidth: 320,
  },
  selectEmpty: {
    marginTop: theme.spacing(2),
  },
  greenColor: {
    color: 'green'
  },
  redColor: {
    color: 'red'
  }
});

export default styles;
